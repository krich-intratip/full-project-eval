// Prompt Generation for Expert Evaluation
// Updated for flexible rubric system and closeout evaluation

import { Rubric, ExpertProfile } from '@/types/rubric';
import { getAllCriteria } from './rubricAdapter';

/**
 * Generate expert evaluation prompt based on rubric
 */
export function generateExpertPrompt(
    rubric: Rubric,
    expert: ExpertProfile,
    pdfText: string
): string {
    const allCriteria = getAllCriteria(rubric);
    const isCloseout = rubric.metadata.context === 'military-closeout';

    // Build scoring instructions based on rubric structure
    const scoringInstructions = rubric.categories.map(cat => {
        const criteriaList = cat.criteria.map(c => {
            const levelDesc = c.scoreLevels.map(l =>
                `${l.min}-${l.max}: ${l.label}`
            ).join(', ');
            return `  - ${c.id} ${c.name} (คะแนนเต็ม ${c.maxScore}): ${levelDesc}`;
        }).join('\n');

        return `หมวด ${cat.number}: ${cat.name} (${cat.maxScore} คะแนน)\n${criteriaList}`;
    }).join('\n\n');

    // Build JSON structure for scores
    const scoresStructure = allCriteria.map(c =>
        `    {"criterionId": "${c.id}", "score": [0-${c.maxScore}], "reason": "เหตุผลการให้คะแนน"}`
    ).join(',\n');

    // Build decision options
    const decisionOptions = rubric.decisionLevels.map(d => d.decision).join('/');

    // Build principles if available
    const principlesSection = rubric.scoringPrinciples && rubric.scoringPrinciples.length > 0
        ? `\nหลักการประเมิน:\n${rubric.scoringPrinciples.map(p => `- ${p}`).join('\n')}\n`
        : '';

    // Context-specific intro and instructions
    let contextIntro: string;
    let evaluationGuidance: string;

    if (isCloseout) {
        contextIntro = `คุณได้รับมอบหมายให้ประเมินผลสัมฤทธิ์โครงการวิจัยและพัฒนาทางทหาร (ขั้นปิดโครงการ) ต่อไปนี้:

สำคัญมาก: เอกสารประกอบด้วย 2 ส่วน
1. "ไฟล์คำขอโครงการ" - คือข้อเสนอโครงการที่ได้รับอนุมัติ (มีวัตถุประสงค์, ขอบเขตงาน, งบประมาณที่ขออนุมัติ)
2. "ไฟล์โครงการฉบับสมบูรณ์" - คือรายงานผลการดำเนินงานจริง พร้อมหลักฐานประกอบ

กรุณาเปรียบเทียบระหว่าง "สิ่งที่ตั้งใจจะทำ" (จากไฟล์คำขอ) กับ "สิ่งที่ทำได้จริง" (จากไฟล์ฉบับสมบูรณ์) เพื่อประเมินว่าโครงการบรรลุวัตถุประสงค์หรือไม่`;

        evaluationGuidance = `
แนวทางการประเมินปิดโครงการ:
- หมวด 1 (การบรรลุวัตถุประสงค์): เปรียบเทียบ deliverables ที่สัญญาไว้กับที่ส่งมอบจริง, ตรวจสอบผลทดสอบ, ดูการรับรองจากหน่วยใช้
- หมวด 2 (ความมีประโยชน์): ประเมินผลกระทบจริงที่เกิดขึ้น, องค์ความรู้/IP ที่ได้, ความพร้อมขยายผล
- หมวด 3 (รายงาน): ความสมบูรณ์ของรายงาน, การวิเคราะห์ข้อมูลที่เชื่อถือได้
- หมวด 4 (ความคุ้มค่า): เปรียบเทียบการใช้จ่ายจริงกับแผน, ความคุ้มค่าเทียบการนำเข้า
`;
    } else if (rubric.metadata.context === 'military') {
        contextIntro = 'คุณได้รับมอบหมายให้ประเมินโครงการวิจัยและพัฒนาทางทหาร (ขั้นกลั่นกรอง) ต่อไปนี้:';
        evaluationGuidance = '';
    } else {
        contextIntro = 'คุณได้รับมอบหมายให้รีวิวงานวิจัยทางวิชาการต่อไปนี้:';
        evaluationGuidance = '';
    }

    return `คุณคือ "${expert.name}" - ${expert.title}

ประสบการณ์: ${expert.experience}
จุดเน้นในการประเมิน: ${expert.focus}
คำถามหลักที่ใช้: ${expert.questions}
${principlesSection}
${contextIntro}
${evaluationGuidance}
===== เอกสาร =====
${pdfText}
===== สิ้นสุดเอกสาร =====

กรุณาประเมินตามเกณฑ์ต่อไปนี้ (คะแนนเต็มรวม ${rubric.totalMaxScore} คะแนน):

${scoringInstructions}

กรุณาตอบกลับในรูปแบบ JSON ที่ถูกต้องตามโครงสร้างนี้เท่านั้น (ไม่ต้องมี markdown code block):

{
  "expertId": "${expert.id}",
  "paperTitle": "ชื่อโครงการ/งานวิจัยที่อ่านได้จากเอกสาร",
  "authors": "ชื่อผู้รับผิดชอบ/หัวหน้าโครงการ/หน่วยงาน",
  "publicationReadiness": "${decisionOptions}",
  "overallComment": "ความเห็นโดยรวมต่อโครงการนี้จากมุมมองของคุณ (3-5 ประโยค)${isCloseout ? ' โดยเน้นการเปรียบเทียบระหว่างแผนกับผลลัพธ์จริง' : ''}",
  "scores": [
${scoresStructure}
  ],
  "strengths": ["จุดแข็ง 1 (ระบุเฉพาะเจาะจง)", "จุดแข็ง 2", "จุดแข็ง 3", "จุดแข็ง 4"],
  "weaknesses": ["จุดอ่อน 1 (ระบุเฉพาะเจาะจง)", "จุดอ่อน 2", "จุดอ่อน 3", "จุดอ่อน 4"],
  "recommendations": [
    {"priority": "critical", "title": "หัวข้อคำแนะนำ", "detail": "รายละเอียดการแก้ไข (เฉพาะเจาะจง)", "expectedResult": "ผลที่คาดหวังเมื่อแก้ไขแล้ว"},
    {"priority": "high", "title": "หัวข้อ", "detail": "รายละเอียด", "expectedResult": "ผลที่คาดหวัง"},
    {"priority": "enhancement", "title": "หัวข้อ", "detail": "รายละเอียด", "expectedResult": "ผลที่คาดหวัง"}
  ],
  "summaryQuote": "คำพูดสั้นๆ สรุปความเห็น (1-2 ประโยค)"
}

สำคัญมาก: ตอบเฉพาะ JSON เท่านั้น ไม่ต้องมีข้อความอื่นใดก่อนหรือหลัง JSON`;
}

/**
 * Generate document summary prompt
 */
export function generateSummaryPrompt(rubric: Rubric, pdfText: string): string {
    const isCloseout = rubric.metadata.context === 'military-closeout';

    if (isCloseout) {
        return `คุณเป็นผู้เชี่ยวชาญด้านการวิเคราะห์โครงการวิจัยและพัฒนาทางทหาร

สำคัญ: เอกสารนี้ประกอบด้วย 2 ส่วน
1. "ไฟล์คำขอโครงการ" - ข้อเสนอที่ได้รับอนุมัติ
2. "ไฟล์โครงการฉบับสมบูรณ์" - รายงานผลการดำเนินงานจริง

กรุณาวิเคราะห์และสรุปเปรียบเทียบระหว่างแผนกับผลลัพธ์:

===== เอกสาร =====
${pdfText.substring(0, 20000)}
===== สิ้นสุดเอกสาร =====

กรุณาสรุปเป็น HTML โดยใช้โครงสร้างนี้ (ตอบเฉพาะ HTML ไม่ต้องมี markdown):

<div class="space-y-4">
  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">📌 ข้อมูลโครงการ</h4>
    <p><strong>ชื่อโครงการ:</strong> [ชื่อ]</p>
    <p><strong>หน่วยรับผิดชอบ:</strong> [ข้อมูล]</p>
    <p><strong>งบประมาณที่ได้รับ:</strong> [จำนวน]</p>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">🎯 วัตถุประสงค์ที่ตั้งไว้ (จากคำขอ)</h4>
    <ul class="list-disc pl-5">
      <li>[วัตถุประสงค์ 1]</li>
      <li>[วัตถุประสงค์ 2]</li>
    </ul>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">✅ ผลลัพธ์ที่ทำได้จริง (จากฉบับสมบูรณ์)</h4>
    <ul class="list-disc pl-5">
      <li>[ผลลัพธ์ 1]</li>
      <li>[ผลลัพธ์ 2]</li>
    </ul>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">📊 การเปรียบเทียบ แผน vs ผลจริง</h4>
    <table class="w-full border-collapse">
      <tr class="bg-gray-100"><th class="border p-2 text-left">รายการ</th><th class="border p-2">ตามแผน</th><th class="border p-2">ผลจริง</th><th class="border p-2">สถานะ</th></tr>
      <tr><td class="border p-2">[รายการ 1]</td><td class="border p-2">[แผน]</td><td class="border p-2">[จริง]</td><td class="border p-2">[ครบ/ไม่ครบ]</td></tr>
    </table>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">💡 ประเด็นสำคัญที่พบ</h4>
    <ul class="list-disc pl-5">
      <li><strong>สิ่งที่ทำได้ดี:</strong> [รายละเอียด]</li>
      <li><strong>สิ่งที่ขาด/ต้องเพิ่ม:</strong> [รายละเอียด]</li>
      <li><strong>ความแตกต่างจากแผน:</strong> [รายละเอียด]</li>
    </ul>
  </div>
</div>

หากข้อมูลไหนไม่พบในเอกสาร ให้ระบุว่า "[ไม่พบข้อมูลในเอกสาร]"
สำคัญ: ตอบเฉพาะ HTML เท่านั้น ไม่ต้องมี markdown code blocks`;
    }

    const contextLabel = rubric.metadata.context === 'military'
        ? 'โครงการวิจัยและพัฒนาทางทหาร'
        : 'งานวิจัยทางวิชาการ';

    return `คุณเป็นผู้เชี่ยวชาญด้านการวิเคราะห์เอกสาร${contextLabel}

กรุณาวิเคราะห์และสรุปเอกสารต่อไปนี้เป็นประเด็นสำคัญ:

===== เอกสาร =====
${pdfText.substring(0, 15000)}
===== สิ้นสุดเอกสาร =====

กรุณาสรุปเป็น HTML โดยใช้โครงสร้างนี้ (ตอบเฉพาะ HTML ไม่ต้องมี markdown):

<div class="space-y-4">
  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">📌 ข้อมูลทั่วไป</h4>
    <p><strong>ชื่อเรื่อง:</strong> [ชื่อ]</p>
    <p><strong>ผู้รับผิดชอบ/สังกัด:</strong> [ข้อมูล]</p>
    <p><strong>ประเภท:</strong> [ประเภท]</p>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">🎯 วัตถุประสงค์</h4>
    <p>[สรุปวัตถุประสงค์]</p>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">🔍 วิธีการดำเนินงาน</h4>
    <p>[สรุปวิธีการ]</p>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">📈 ผลลัพธ์ที่คาดหวัง/ผลการวิจัย</h4>
    <ul class="list-disc pl-5">
      <li>[ผลลัพธ์ที่สำคัญ 1]</li>
      <li>[ผลลัพธ์ที่สำคัญ 2]</li>
    </ul>
  </div>

  <div class="bg-white p-4 rounded-lg border">
    <h4 class="font-semibold text-lg mb-2">💡 ประเด็นสำคัญที่พบ</h4>
    <ul class="list-disc pl-5">
      <li><strong>จุดแข็ง:</strong> [สิ่งที่ทำได้ดี]</li>
      <li><strong>จุดที่ต้องปรับปรุง:</strong> [สิ่งที่ขาด/ต้องเพิ่ม]</li>
    </ul>
  </div>
</div>

หากข้อมูลไหนไม่พบในเอกสาร ให้ระบุว่า "[ไม่พบข้อมูลในเอกสาร]"
สำคัญ: ตอบเฉพาะ HTML เท่านั้น ไม่ต้องมี markdown code blocks`;
}

/**
 * Generate improved draft prompt
 */
export function generateImprovedDraftPrompt(
    rubric: Rubric,
    projectName: string,
    organizationName: string,
    totalScore: number,
    percentage: number,
    recommendations: Array<{ priority: string; title: string; detail: string; source: string }>,
    strengths: string[],
    weaknesses: string[]
): string {
    const contextLabel = rubric.metadata.context === 'military' || rubric.metadata.context === 'military-closeout'
        ? 'โครงการวิจัยและพัฒนาทางทหาร'
        : 'งานวิจัยทางวิชาการ';

    return `คุณเป็นผู้เชี่ยวชาญด้านการเขียนเอกสาร${contextLabel} กรุณาสร้างร่างเอกสารฉบับปรับปรุงใหม่ โดยนำคำแนะนำจากผู้ทรงคุณวุฒิมาปรับใช้

ข้อมูลเดิม:
- ชื่อเรื่อง: ${projectName}
- สังกัด: ${organizationName}
- คะแนนรวม: ${totalScore.toFixed(1)}/${rubric.totalMaxScore} (${percentage.toFixed(1)}%)

คำแนะนำจากผู้ทรงคุณวุฒิที่ต้องนำไปปรับปรุง:
${recommendations.map(r => `- [${r.priority}] ${r.title}: ${r.detail} (จาก: ${r.source})`).join('\n')}

จุดแข็งที่ต้องรักษา:
${strengths.slice(0, 5).map(s => `- ${s}`).join('\n')}

จุดอ่อนที่ต้องแก้ไข:
${weaknesses.slice(0, 5).map(w => `- ${w}`).join('\n')}

กรุณาสร้างเอกสารฉบับปรับปรุงในรูปแบบ HTML (ตอบเป็น HTML เท่านั้น ไม่ต้องมี markdown):

<div class="space-y-6">
  <div class="bg-white p-6 rounded-lg border">
    <h3 class="font-bold text-xl mb-4 text-blue-800">1. ส่วนที่ปรับปรุง</h3>
    <div class="space-y-2">
      <p><strong>ชื่อเรื่อง (ปรับปรุงให้ชัดเจน):</strong></p>
      <p>[ชื่อเรื่องใหม่]</p>
      <p><strong>บทคัดย่อ/สรุปโครงการ:</strong></p>
      <p>[เนื้อหาที่ปรับปรุง]</p>
    </div>
  </div>

  <!-- ส่วนอื่นๆ ตามโครงสร้างมาตรฐาน -->
</div>

สำหรับข้อมูลที่ไม่สามารถอนุมานได้ ให้ใส่:
<div class="bg-orange-50 border-l-4 border-orange-500 p-3 my-2">
  <strong>ต้องเพิ่มข้อมูล:</strong> [อธิบายว่าต้องเพิ่มอะไร]
</div>

ทุกส่วนต้องเขียนให้ได้คะแนนสูง ใช้ภาษาไทยทั้งหมด`;
}
