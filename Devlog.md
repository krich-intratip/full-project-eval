# 📋 Development Log - Academic SAR

บันทึกการพัฒนาและแก้ไขปัญหาของโครงการ Academic SAR

---

## 2026-01-15: แก้ไขปัญหาหน้าเว็บค้างไม่แสดงผล

### 🔴 ปัญหาที่พบ
- หน้าเว็บค้าง ไม่แสดงผลอะไรเลยเมื่อเปิด localhost:3000
- Dev server ไม่สามารถเริ่มต้นได้อย่างถูกต้อง

### 🔍 สาเหตุ
- มี **Node.js processes หลายตัว**ที่ทำงานพร้อมกัน
- มี lock file อยู่ใน `.next/dev/lock` ทำให้ Next.js ไม่สามารถเริ่มต้นได้
- Error message: `Unable to acquire lock at .next/dev/lock, is another instance of next dev running?`

### ✅ วิธีแก้ไขที่สำเร็จ

1. **ปิด Node.js processes ทั้งหมด:**
   ```cmd
   cmd /c "taskkill /F /IM node.exe"
   ```

2. **เริ่ม dev server ใหม่:**
   ```cmd
   cmd /c "npm run dev"
   ```

### ❌ วิธีที่ไม่สำเร็จ / ควรหลีกเลี่ยง

1. **อย่าใช้ PowerShell โดยตรง** - มีปัญหา execution policy:
   ```
   npm : File C:\Program Files\nodejs\npm.ps1 cannot be loaded because running scripts is disabled
   ```
   **ใช้ `cmd /c "..."` แทน**

2. **อย่าลอง run dev server ซ้ำ** โดยไม่ปิด process เดิมก่อน - จะทำให้เกิด lock conflict

### 📝 คำสั่งที่ควรจำ

| สถานการณ์ | คำสั่ง |
|-----------|--------|
| ปิด Node.js ทั้งหมด | `cmd /c "taskkill /F /IM node.exe"` |
| ลบ cache และ lock | `cmd /c "rmdir /s /q .next"` |
| เริ่ม dev server | `cmd /c "npm run dev"` |
| ตรวจสอบ port ที่ใช้งาน | `cmd /c "netstat -ano | findstr :3000"` |

### 🎯 ผลลัพธ์
- หน้าเว็บแสดงผลได้อย่างถูกต้องที่ `http://localhost:3000`
- ทุก component ทำงานได้ปกติ (Navigation, QR Code, AI Provider, PDF Upload)

---

## ⚙️ ข้อมูลโครงการ

| รายการ | ค่า |
|--------|-----|
| Framework | Next.js 16.1.1 (Turbopack) |
| Version | v2.1.0 |
| Port | 3000 (default) |
| Last Updated | 15 มกราคม 2569 |

---

## 📌 Tips สำหรับการพัฒนา

1. **ก่อนเริ่ม dev server ทุกครั้ง** - ตรวจสอบว่าไม่มี Node.js process ค้างอยู่
2. **ใช้ cmd แทน PowerShell** สำหรับ npm commands บน Windows
3. **ถ้าเกิด lock error** - ปิด processes ทั้งหมด + ลบ .next folder
4. **Hydration warning** (minor) - เกิดจาก browser extensions inject attributes ไม่กระทบการทำงาน
